<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.loosu.activityanimation">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.TestActivity"
            android:theme="@style/AppTheme.TransparentWindow" />

        <activity
            android:name=".ui.TransitionActivity"
            android:theme="@style/AppTheme.TransparentWindow" />
        <activity android:name=".ui.gallery.GridGalleryActivity" />
        <activity android:name=".ui.gallery.ViewPagerGalleryActivity" />
        <activity android:name=".ui.yc.activity.YcGridGalleryActivity" />
        <activity android:name=".ui.yc.activity.YcViewPagerGalleryActivity" />
        <activity android:name=".ui.carousel.CarouselActivity" />
        <activity android:name=".ui.carousel.CarouselDetailActivity" />
    </application>

</manifest>